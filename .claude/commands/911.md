---
name: 911
description: 'Quick cheatsheet — Daily 4 commands + decision tree + first-time setup. ~3K tokens. For full list (planning, on-demand, auto, internal): /911-full.'
model: sonnet
---

**INSTRUCTIONS:**
1. Project language is in your already-loaded CLAUDE.md `## Language` section. Do NOT call Read tool.
2. Print cheatsheet below. Translate prose to project language; keep command names, file paths, tool names, code blocks, technical abbreviations (TDD, ADR, PRD, MCP) untouched.
3. No preface, no summary, no "What's next?".

---

# 911 — Quick cheatsheet

> Forgot a command? Start here. Need full list? `/911-full`.

## 🟢 Daily 4 (memorize)

| Command | One-liner |
|---|---|
| `/todo add <description>` | Spec-first task with grill-me + Diablo gates |
| `/orchestrate` | Autonomous backlog: test-writer → tests → review → Diablo → commit |
| `/general <question>` | Verified Q&A, evidence-first, no speculation |
| `/rule <statement>` | Capture business rule (rate / fee / formula) into docs/RULES.md |

## 🔵 Decision tree

```
Want to know something          → /general
Found a bug                      → /fix
Have a vague new idea            → /intent → /decompose
Have one specific task           → /todo add → /orchestrate
Want to ship something           → /orchestrate
Need a sanity check              → /gaps both
Code feels wrong                 → /improve-arch
Just deployed — check prod ok?   → /canary <production_url>
Building API/CLI/SDK for devs    → /plan-devex-review
Forgot a command                 → /911-full (FULL cheatsheet)
```

## 🟡 Setup & first-time

| Command | When |
|---|---|
| `/setup` | First time on machine; after token rotation; periodic health check |
| `/init-project [path]` | Scaffold new project from template |
| `/911-full` | Full cheatsheet (planning, on-demand, auto-via-launchd, internal) |

---

For full list of `/intent`, `/decompose`, `/fix`, `/review`, `/da`, `/improve-arch`, `/council`, `/gaps`, `/test`, `/report`, `/docs`, `/self-audit`, `/canary`, `/plan-devex-review`, `/todo done|start|list` — run `/911-full`.
